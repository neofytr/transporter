# Vendored nlohmann/json single header (MIT). Fallback when the system
# package is unavailable. Include dir is this directory so the canonical
# <nlohmann/json.hpp> spelling resolves via the nlohmann/ subdir.

nlohmann_vendored_dep = declare_dependency(
  include_directories : include_directories('.'),
)
